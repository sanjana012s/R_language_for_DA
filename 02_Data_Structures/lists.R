#A list in R is a data structure that can store different types of elements (numbers, strings, vectors, matrices, etc.) in a single object.
#syntax:my_list <- list(element1, element2, element3)
my_list <- list(name="sanjna",age=20,marks=89)
print(my_list)
