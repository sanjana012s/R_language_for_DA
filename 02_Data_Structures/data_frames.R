#A data frame is a table-like structure that stores data in rows and columns, where each column can have different data types.
#Syntax: data.frame(column1, column2, column3)
df <- data.frame(id = 1:3, name = c("A", "B", "C"))
print(df)